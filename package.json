{
  "name": "ukulele-studio",
  "version": "1.0.0",
  "description": "Ukulele Practice App — chord sync, lyrics, metronome",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "firebase-admin": "^13.10.0",
    "jsdom": "^24.0.0",
    "vitest": "^2.0.0"
  }
}
